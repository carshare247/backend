package com.carpool.repository;

import com.carpool.entity.RewardSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RewardSettingsRepository extends JpaRepository<RewardSettings, Short> {
}
